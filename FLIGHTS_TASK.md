# הבדיקה היומית של הטיסות — הוראות הרצה

מסמך זה הוא ה-runbook של המשימה היומית. הוא נכתב כדי שכל הרצה תהיה זהה ואמינה.

## המטרה

לחפש **טיסות ישירות בלבד** מתל אביב (TLV) לטוקיו וחזרה, בחלון **21/06/2027 – 31/07/2027**,
לעדכן את `japan_trip_planner.html`, ולשמור היסטוריית מחירים כדי שנראה עליות/ירידות.

## כלל הברזל

> **אסור לכתוב מחיר שלא נקרא מדף שנטען בפועל באותה הרצה.**
> לא להעריך, לא לזכור, לא להשלים "מחיר טיפוסי". אם לא הצלחת לקרוא מחירים בהרצה מסוימת —
> עדכן רק את `lastCheckedLabel` והוסף הערה, אל תמציא מספרים. מחיר מומצא הורס את כל התוצר.

## עובדות רקע שכבר בוססו (07/08/2026)

- **שתי חברות בלבד** מפעילות קו ישיר TLV–טוקיו נריטה (NRT): **אל על** ו**ארקיע**.
  ארקיע נכנסה לקו באוקטובר 2026, פעמיים בשבוע, במטוס A330.
- **הקו לא מופעל כל יום.** נבדק ואומת שאין טיסה ישירה ב-21/06/2027 (ב׳), 27/06/2027 (א׳), 04/07/2027 (א׳).
  יש טיסה ישירה ב-23/06/2027 (ד׳).
- **אופק המכירה הוא כ-330 יום.** תאריכים רחוקים יותר פשוט לא קיימים במערכת עדיין.
  נכון ל-07/08/2026 זה אומר שהחלק השני של החלון (מאמצע יולי 2027 והלאה) עדיין לא נפתח.
  **כל יום נפתח עוד יום בקצה** — לכן חשוב לסרוק גם את הקצה הרחוק בכל הרצה.
- אל על אינה טסה בשבת ובחגים. ארקיע היא הישראלית שהכי סביר שתטוס בשבת.

## שלבי ההרצה

### 1. סריקה

השתמש ב-KAYAK עם פילטר "ישירות בלבד" דרך ה-URL:

```
https://il.kayak.com/flights/TLV-NRT/{YYYY-MM-DD}/{YYYY-MM-DD}?sort=price_a&fs=stops=0
```

- הדף נטען ב-JavaScript. **חובה כלי דפדפן אמיתי** (`mcp__Claude_Browser__navigate` ואז
  `get_page_text`). `WebFetch` יחזיר שלד ריק בלי מחירים.
- אחרי הניווט **המתן כ-10 שניות** לפני הקריאה, אחרת תתפוס "Fetching prices...".
- אם מופיע `No matching flights found` — אין ישירה בתאריכים האלה. רשום את התאריך
  ב-`noNonstop` ואל תמציא לו מחיר.

**אילו זוגות תאריכים לסרוק:** הטיול הוא 14 יום. סרוק זוגות של יציאה+חזרה במרווח 14 יום,
שבהם היציאה נופלת בין 21/06/2027 ל-17/07/2027. כדי לחסוך זמן, סרוק קודם את ימי השבוע
שכבר ידוע שמופעלים (רביעי), ואז נסה למפות ימים נוספים בחיפוש כיוון-אחד:

```
https://il.kayak.com/flights/TLV-NRT/{YYYY-MM-DD}?sort=price_a&fs=stops=0
```

**תמיד** סרוק גם את הזוג הרחוק ביותר שנפתח היום, כדי לתפוס מלאי חדש.

### 2. הצלבה

אל תסתפק במקור אחד. הצלב לפחות מול אחד מ:
- אל על ישירות: https://www.elal.com/flight-deals/en-il/flights-from-tel-aviv-to-tokyo
- Skyscanner תצוגת חודש: `https://www.skyscanner.co.il/transport/flights/tlv/nrt/{YYMM}00/`

### 3. עדכון הקובץ

ערוך את בלוק ה-JSON `<script id="flightsData" type="application/json">` בתוך
`japan_trip_planner.html`:

- `lastCheckedISO` ו-`lastCheckedLabel` — לזמן ההרצה.
- לכל מסלול קיים: **הוסף רשומה** ל-`priceHistory` בצורה `{ "d": "YYYY-MM-DD", "p": <מחיר> }`.
  **אל תמחק** רשומות ישנות — הן מה שמייצר את החץ והשוואת המחיר.
- מסלול חדש שנפתח: הוסף אובייקט חדש ל-`options` לפי אותו מבנה.
- מסלול שנעלם מהמכירה: השאר אותו והוסף `"gone": true` בהערה, אל תמחק היסטוריה.
- עדכן את `noNonstop` בתאריכים שנבדקו ואין בהם ישירה.
- אם אומתה מדיניות מזוודות/ארוחות — העבר פריטים מ-`unknown` ל-`included` או ל-`toPay`
  עם המחיר שנקרא.

הרינדור, כולל החצים, מתבצע אוטומטית ב-`renderFlights()` — אין צורך לגעת בו.

### 4. סיכום ו-commit

- אם מחיר ירד מתחת ל-**₪3,700 לנוסע** — זה אירוע שכדאי להתריע עליו בסיכום ההרצה.
- עשה commit עם הודעה בסגנון `Daily flight check YYYY-MM-DD`.

## מה עדיין פתוח לאימות

- [ ] מדיניות מזוודות של **ארקיע בקו הארוך לטוקיו** (הטבלה המוכרת שלה היא לקווים לאירופה — לא להניח שהיא זהה).
- [ ] אילו ארוחות כלולות אצל ארקיע בקו הזה.
- [ ] באיזו מחלקת כרטיס (Lite / Classic) נמכר המחיר של אל על שנמצא — זה קובע אם מזוודה כלולה.
- [ ] מיפוי מלא של ימי ההפעלה בשבוע של שתי החברות לקיץ 2027.
