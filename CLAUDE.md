# CLAUDE.md — פרויקט "Japan" (טיול משפחתי ליפן, יולי 2027)

זיכרון פרויקט קבוע. נטען אוטומטית בכל שיחה בתיקייה הזו.
**כלל עבודה:** כל החלטה או שינוי משמעותי → לעדכן את הקובץ הזה ואת `DECISIONS.md`.

---

## 1. הקשר

- טיול משפחתי בן **14 יום ליפן, יולי 2027**.
- המשפחה: **זוג הורים + ילד בן 13**, חובב **פוקימון וגיימינג** (זה מניע חלק גדול מהמסלול: מרכזי פוקימון, Nintendo Tokyo, אקיהברה, Super Nintendo World ב-USJ).
- התכנון התחיל בשיחה ארוכה עם Claude במצב Cowork, ועבר לכאן כדי שיהיה זיכרון קבוע של הקוד **ושל ההיסטוריה/ההחלטות**.

## 2. תוצרים

| קובץ | תיאור |
|---|---|
| `japan_trip_planner.html` | **התוצר החי היחיד** — אתר HTML אינטראקטיבי בעברית, RTL, קובץ יחיד |
| `CLAUDE.md` | הקובץ הזה — זיכרון פרויקט |
| `DECISIONS.md` | יומן החלטות כרונולוגי |
| `FLIGHTS_TASK.md` | runbook של הבדיקה היומית של הטיסות — **לקרוא לפני כל נגיעה בנתוני טיסות** |

**אין ואל תיצור PDF.** הפורמט הנבחר הוא HTML בלבד. (בשיחות קודמות דובר גם על PDF — זה בוטל.)

## 3. מבנה `japan_trip_planner.html`

קובץ יחיד עצמאי (HTML+CSS+JS inline). תלויות חיצוניות ב-CDN: Google Fonts (Heebo), Leaflet 1.9.4 (cdnjs), אריחי OpenStreetMap.

`<html lang="he" dir="rtl">`. ארבעה טאבים, מוחלפים ב-`switchTab(id)`:

| טאב | `<section id>` | תוכן |
|---|---|---|
| 🏠 ראשי | `tab-home` | תמצית, 4 כרטיסי סטטיסטיקה, מד התקדמות כולל, סיכום הבדיקות הישראליות, כרטיסי ניווט |
| 🗺️ מסלול ומפה | `tab-map` | **3 כרטיסי הצעות מסלול** (`.route-card`) בראש הטאב, ואז מפת Leaflet + `city-block`/`day-card` לכל עיר, עם צ'קבוקס `.dayChk` לכל יום |
| ✈️ טיסות | `tab-flights` | באנר "בדיקה אחרונה", כרטיסי טיסה עם חצי שינוי מחיר, תאריכים בלי ישירה, המלצת קנייה, קישורים חיים |
| 💰 תקציב | `tab-budget` | תיבת שער חליפין, גרף עמודות, טבלת `table.budget` מלאה |
| ✅ צ'ק-ליסט | `tab-checklist` | קופסת "הערה" (`.note-box`, `sendNote()`) → מייל, ציר זמן הכנות, קבוצות משימות עם `.ckItem`, רשת קישורים |

### נקודות מפתח בקוד (למי שבא לערוך)

- **כרטיסי הצעות המסלול** — בראש `#tab-map`. שלושה `.route-card` (ב' מסומן `.is-rec` = ההמלצה), כל אחד עם `.route-legs` (שרשרת התחנות), `.pc-grid` (יתרונות/חסרונות), ו-`.route-rec` (ההמלצה). **כשתיבחר הצעה — להחליף את הבלוק הזה בסיכום קצר של הנבחרת**, לא להשאיר שלוש.
- **`CITY_PINS`** (~שורה 819) — 4 פינים ראשיים שתמיד מוצגים: `tokyo`, `kyoto`, `kyoto2` (=נארה, עם `linkKey:'kyoto'`), `osaka`. לכל אחד `coords`, `color`, `icon`, `name`, `desc`.
- **`DAYTRIP_PINS`** (~830) — ניקקו / הקונה / קמאקורה, פינים קטנים שתמיד מוצגים, מחוברים לטוקיו בקווים מנוקדים.
- **`DETAIL_PINS`** (~839) — ~20 אתרים ספציפיים. `cat` הוא אחד מ-`poke` (⚡ צהוב `#e0a300`), `hotel` (🏨 אפור `#5b6883`), `sight` (📍 בצבע העיר).
- **`initLeafletMap()`** (~907) — בונה את המפה, מצייר `L.polyline` של המסלול (`CITY_PINS[0] → [1] → [3]`), ספור קיוטו↔נארה, ומחברי יום-טיול. הפינים המפורטים ב-`detailLayer` שמופיע/נעלם ב-**`ZOOM_THRESHOLD = 10`** (`syncDetailLayer`).
- **`focusCity(key)`** (~968) — יש בו **אובייקט `info` משוכפל** עם אותם טקסטים כמו ב-`CITY_PINS.desc`. **בכל עדכון מסלול צריך לעדכן את שניהם**, אחרת הם יסתרו.
- **`updateAllProgress()`** (~1002) — סופר `.dayChk` ו-`.ckItem` ומעדכן 3 מדי התקדמות.
- **`#flightsData`** — בלוק `<script type="application/json">` עם כל נתוני הטיסות והיסטוריית המחירים. **מקור האמת היחיד**; המשימה היומית עורכת אותו בלבד. `renderFlights()` בונה ממנו את הטאב ומחשב את חץ השינוי משתי הרשומות האחרונות ב-`priceHistory`. **אף פעם לא למחוק רשומות היסטוריה** — בלעדיהן אין השוואת מחיר.
- המפה מאותחלת עצלנית בכניסה הראשונה לטאב מפה (`switchTab` קורא ל-`initLeafletMap`, אחרת `invalidateSize`).
- **בלוק Firebase** (`<script type="module">`, ממש לפני `</body>`) — מכיל את `firebaseConfig` (לא סוד, מותר גלוי), ומחבר צ'ק-ליסט משותף (`data-syncKey` על כל `.dayChk`/`.ckItem` → `doc('checklist','state')`) ויומן הערות (`#noteLog` ← collection `notes`). ראו סעיף 8.

### כללי עברית / RTL — חשוב

- כל הטקסט בעברית תקנית, RTL מלא.
- **`class="ltr-num"`** (`direction:ltr; unicode-bidi:isolate; display:inline-block`) — **חובה** סביב כל טווח מספרי, שבר, או מספר מעורב עם אנגלית (`23,000–40,000`, `1–7`, `$7,590–13,200`, `0 / 14`). זה היה מקור באגים חוזר בעבר.
- `#leafletMap` מקבל `direction: ltr` בכוונה, כדי שכפתורי ה-+/− של Leaflet יישארו במקומם.
- להשתמש בתכונות לוגיות (`inset-inline-start`, `padding-inline-start`, `margin-inline-start`) ולא ב-left/right.

## 4. הבדיקות הישראליות

נבדקו **אוגוסט 2026**. הסטטוס שנמצא אז:

- **ויזה:** אזרחי ישראל פטורים מוויזה לשהות תיירותית קצרה ביפן.
- **אזהרת מסע:** רמה 1 (איום נמוך).
- **דרכון קטין (מתחת לגיל 16):** תוקף **5 שנים בלבד**, לא 10 — לבדוק את תוקף דרכון הילד מראש.
- **ביטוח נסיעות נפרד — חובה.** קופ"ח לא מכסה טיפול רפואי בחו"ל.

> ⚠️ **כלל קשיח:** אף פעם אל תסתמך על זיכרון, על הקובץ הזה, או על שיחה קודמת לגבי **ויזה / אזהרות מסע / מחירים**. בכל פעם שהנושא עולה שוב — לאמת מחדש מול המקורות הרשמיים: המל"ל (nsc.gov.il), משרד החוץ (gov.il), שגרירות יפן בישראל. הנתונים האלה משתנים.

הסקיל הרלוונטי: `israeli-abroad-trip-planner` (מבצע את הבדיקות בזמן אמת), ו-`israeli-flight-finder` להשוואת טיסות.

## 4א. טיסות — מה בוסס (07/08/2026)

- **רק שתי חברות** מפעילות קו ישיר TLV–טוקיו נריטה: **אל על** ו**ארקיע** (ארקיע נכנסה לקו באוקטובר 2026, פעמיים בשבוע, A330).
- **הקו לא מופעל כל יום** — אומת שאין ישירה ב-21/06/2027, 27/06/2027, 04/07/2027; יש ב-23/06/2027.
- **אופק המכירה ~330 יום** — החלק השני של חלון החיפוש (מאמצע יולי 2027) עדיין לא נפתח למכירה, ונפתח בהדרגה יום ביום.
- מחירים ראשונים שנקראו (KAYAK, לנוסע, הלוך-חזור, ישיר, 23/06→07/07/2027): ארקיע ₪3,790 · אל על ₪3,933 · משולבים ₪5,126 ו-₪5,261.
- **שלוש תקופות במעקב**, לכל אחת צבע קבוע שמסמן גם את כרטיסי הטיסה שלה:
  - **קיץ 2027** — 21/06 עד **20/08**, צבע `#d35400`. הזול: **₪3,423** (אל על ישיר, **07/07→20/07, 13 לילות**).
  - **אפריל 2027** — צבע `#c2185b`. הזול: **₪5,720** (ארקיע ישיר, 07/04→21/04, עודכן 08/08/2026). פסח 21–29/04/2027.
  - **אוקטובר 2027** — צבע `#8d6e00`. **לא ניתן לתמחור** — מחוץ לאופק. חגי תשרי 2027: ר"ה 01–03/10, כיפור 10–11/10, סוכות 15–22/10, שמחת תורה 23–24/10.
- **תיקון חשוב (07/08/2026):** ההנחה שמעבר ל-330 יום אין מלאי **הייתה שגויה**. 21/07→04/08/2027 נמכר היטב ואף יצא הזול ביותר. מה שחוסם תאריכים הוא **ימי ההפעלה של הקו**, לא האופק.
- **החיפוש כולל גם קונקשנים** — אך ורק עד **4 שעות המתנה**. בכותרת כל מסלול מוצג זמן המסע הכולל, ומתחתיו פירוט הקטעים (`legs`: `fly`/`wait`).
- **כלל תצוגה:** כרטיס אחד לכל זוג תאריכים. ברירת מחדל הזול, **אבל אם הזול הוא קונקשן ארוך והישירה יקרה ממנו רק במעט — מציגים את הישירה** ומוסיפים `altNote` עם הפרש המחיר, הפרש השעות, ומחיר הקונקשן. המיון לפי מחיר אוטומטי ב-`renderFlights()`.
- **משך הטיול גמיש 12–16 לילות** — גם לקצר וגם להאריך. ביציאה 07/07/2027: 12 לילות ₪3,649 · **13 לילות ₪3,423** · 14 ₪3,573 · 15 ₪3,713 · 16 — אין ישירה.
- **מחיר בדולרים מוצג מתחת לשקלים.** מחושב מ-`fxUsd` בבלוק הנתונים; השער נקרא **חי** בכל בדיקה (xe.com), לעולם לא משער זכור. נכון ל-07/08/2026: `1 USD = 3.00836 ₪`.
- **קצה המכירה:** חזרה עד ~04/08/2027 נמכרת; 11/08 ואילך עדיין נדחה.
- **השלב הנוכחי הוא בחירת טיסות ותאריכים בפועל.** תכנון המסלול יבוא רק אחרי שייסגרו — המסלול נגזר מהתאריכים.
- **Open Jaw נבדק בכל הרצה.** אין ישירה מאוסקה (KIX) לתל אביב. **ב-08/08/2026 התקבל מחיר אמיתי לראשונה:** KIX→TLV ל-20/07/2027, Etihad דרך אבו דאבי (המתנה 2ש׳45ד׳), ₪2,792 לנוסע. בהשוואה מלאה מול הלוך TLV→NRT (₪2,130) — סה"כ Open Jaw ₪4,922 לנוסע, **יקר משמעותית** (כ-₪1,499) מהלוך-חזור הרגיל (₪3,423). המסקנה בשלב זה: לא משתלם, הלולאה חזרה לטוקיו נשארת ההנחה. ממשיכים לעקוב.
- **משימה יומית פעילה:** `japan-daily-flight-check`, רצה כל יום ב-07:29, סורקת את שלוש התקופות ומעדכנת את `#flightsData`. הגדרות ב-`C:\Users\ozs19\.claude\scheduled-tasks\japan-daily-flight-check\SKILL.md`.

## 5. תקציב

7 קטגוריות: טיסות, לינה, תחבורה פנימית, כניסות/אטרקציות, אוכל, ביטוח, שונות.
טווח נוכחי: **≈23,000–40,000 ₪ כולל טיסות** (≈14,000–24,000 ללא).
תצוגה: **₪ כמספר ראשי, $/¥ בסוגריים**.
שער ששימש לחישוב (אוג׳ 2026): `100 JPY ≈ 1.94 ₪ | 1 USD ≈ 3.03 ₪ | 1 USD ≈ 156 JPY`.

⚠️ **טרם נבדק מול מחירים אמיתיים בזמן אמת.** בפרט טיסות — `israeli-flight-finder` עדיין לא הופעל.

## 6. מה השתנה ועדיין לא מוטמע בקבצים ⚠️

**הטיול צריך להתחיל ולהסתיים בטוקיו — לא open-jaw.**

הקובץ הנוכחי עדיין מבוסס על ההנחה הישנה (נחיתה בטוקיו, המראה מ-KIX באוסקה). מקומות שידרשו עדכון כשייבחר מסלול:

1. ה-`day-card`-ים בטאב המפה (טוקיו ימים 1–7, קיוטו 8–11, אוסקה 12–14) — כולל יום 14 שכרגע כתוב בו "נסיעה לשדה קאנסאי (KIX)".
2. ~~ה-notice "המלצת טיסה" שממליץ על Open Jaw~~ — **תוקן ב-06/08/2026**, הוחלף באזהרה שהפירוט למטה עדיין ישן.
3. `CITY_PINS[*].desc` + אובייקט `info` בתוך `focusCity` (טווחי הימים).
4. קווי ה-`L.polyline` ב-`initLeafletMap` — צריך לולאה שחוזרת לטוקיו.
5. `DETAIL_PINS[*].day` — תגיות הימים.
6. הכותרת בהדר, הבאדג'ים, ו-`stat-card` "3 ערים בסיס".

### שלוש הצעות המסלול (טרם נבחרה אחת)

שלושתן **מוצגות באתר** בראש טאב "מסלול ומפה", עם יתרונות/חסרונות והמלצה לכל אחת. ההמלצה שניתנה: **ב'**.

- **א' — "לולאה קלאסית":** טוקיו 1–5 → קיוטו+נארה 6–9 → אוסקה+USJ 10–12 → חזרה לטוקיו ברכבת 13–14.
  *יתרון:* טיסה פשוטה מאותו שדה, "סיבוב שני" בטוקיו בסוף. *חיסרון:* יומיים נסיעה הלוך-חזור.
- **ב' — "לולאה עם עצירה בהקונה":** טוקיו 1–5 → קיוטו+נארה 6–9 → אוסקה+USJ 10–12 → הקונה 13 (לילה בריוקאן עם אונסן, נוף להר פוג'י) → טוקיו 14.
  *יתרון:* לולאה גיאוגרפית הגיונית, לא חוזרים על אותו קטע מסילה, גיוון כפרי/הררי בדרך.
- **ג' — "לולאה מורחבת" דרך האלפים היפניים:** טוקיו 1–4 → קיוטו+נארה 5–8 → אוסקה+USJ 9–10 → טקאיאמה/שיראקאווה-גו/קנזאווה 11–13 → טוקיו 14.
  *יתרון:* הכי הרבה גיוון נופי/תרבותי, JR Pass כמעט בוודאות משתלם. *חיסרון:* פחות זמן בכל עיר, פחות תוכן פוקימון.

## 7. נושאים פתוחים — **לשאול, לא לנחש**

1. איזה משלושת המסלולים (או שילוב) — **לא הוכרע**.
2. תאריכים מדויקים ביולי 2027 (שיקול: פסטיבל גיון בקיוטו, שיאו סביב 17/7).
3. האם לכלול יום טיול נוסף (ניקקו/קמאקורה) במסלול א' או דומה.
4. סגנון לינה — מלון רגיל מול לפחות לילה אחד בריוקאן/אונסן.
5. JR Pass מול כרטיסים בודדים — תלוי במסלול הסופי.
6. השוואת טיסות אמיתית — טרם בוצעה.
7. הזמנת Pokémon Café מראש (נפתח ~3 חודשים לפני, נגמר מהר).
8. ~~האם לשמור את מצב הצ'ק-ליסט בין כניסות~~ — **הוכרע 08/08/2026: משותף לכל המשפחה** (לא `localStorage` מקומי). ממתין להקמת Firebase, ראו סעיף 8.
9. ~~הקמת repo ב-GitHub~~ — **בוצע 09/08/2026.** האתר חי ב-https://ozs1976.github.io/japan-trip-2027/.

## 8. יעד הפצה + שדרוג האתר (הוחלט 08/08/2026)

המשפחה רוצה **אתר חי**, לא קובץ שנשלח פעם אחת, שילווה אותם גם בפועל בטיול (מובייל, עדכני, דו-כיווני). הוחלט על שלושה שיפורים ב-3 שלבים:

### א. רספונסיביות (מובייל) — ✅ בוצע 08/08/2026
תוקן overflow אופקי אמיתי בטאב התקציב (הטבלה עטופה עכשיו ב-`.table-scroll`, `overflow-x:auto`, עם `min-width:560px` על `table.budget`). נבדק תכנותית (לא רק חזותית) ב-320px/375px/401px רוחב עם סקריפט שמזהה `body.scrollWidth > viewport` — אפס חריגות בכל 5 הטאבים. שאר הרשת כבר הייתה רספונסיבית (`@media (max-width:700px/800px)` על `.grid`, `.pc-grid`, `.legs`, `.period-meta`, `.link-grid`). כפתורי צ'קבוקס בצ'ק-ליסט הוגדלו ל-20px למגע.

### ב. דינמיות — קופסת "הערה" + צ'ק-ליסט משותף
**הוחלט (08/08/2026), אחרי דיון על חלופות (מייל אסינכרוני מול צ'אט AI חי בתשלום) — נבחר מייל אסינכרוני, עם תשתית ל-DB משותף:**

1. **✅ בוצע:** קופסת "הערה" בטאב הצ'ק-ליסט (`.note-box`, פונקציית `sendNote()`). שולחת גם `mailto:ozs1976@gmail.com` וגם כותבת ל-Firestore.
2. **✅ בוצע (08–09/08/2026):** Firebase הוקם (`japan-trip-2027-f7352`, Firestore ב-eur3, Production mode), אפליקציית Web נרשמה, כללי אבטחה פורסמו (פתוחים רק ל-collections `checklist`/`notes`). **חשוב לדעת:** את כללי האבטחה **המשתמש הדביק בעצמו** — חוסם בטיחות מובנה מנע ממני להקליד `allow ... if true` בעצמי, גם עם אישור מפורש. אם יידרש שוב שינוי ב-Security Rules — לבקש מהמשתמש לבצע בעצמו, לא לנסות לעקוף.
3. **✅ בוצע:** Firebase מחובר בפועל ב-`japan_trip_planner.html` (script מודולרי בסוף הקובץ, לפני `</body>`): צ'ק-ליסט/מסלול **משותף לכל המשפחה** בזמן אמת (`.dayChk`/`.ckItem` → `data-syncKey` → `doc('checklist','state')` → `onSnapshot`), ויומן הערות+תשובות ב-`#noteLog` (קורא מ-collection `notes`, שדה `reply` עדיין `null` עד שהבוט ימלא אותו). נבדק קצה-לקצה (רענון דף שומר מצב מסומן).
4. **✅ בוצע (09/08/2026):** משימה מתוזמנת `japan-note-watcher` הוקמה (`mcp__scheduled-tasks__create_scheduled_task` — **לא** ה-skill "schedule"/`RemoteTrigger` שהם routines בענן בלי גישה לקבצים מקומיים/git ועם מינימום שעה בין הרצות; יש הבדל חשוב בין השניים, ראו "מלכודת" למטה). cron: `0,30 7-23 * * *` — כל 30 דקות, רק 07:00–24:00 (לא בלילה). runbook מלא: `NOTE_WATCHER_TASK.md`.
   - **אבטחה — קוד סודי בתחילת המייל, נבחר על ידי קלוד ולא נחשף בקוד/ב-repo:** נשמר **רק** בקובץ מקומי `C:\Users\ozs19\.claude\scheduled-tasks\japan-note-watcher\secret.txt` (מחוץ לתיקיית הפרויקט הציבורית!). הבוט קורא את הקוד משם בכל הרצה, משווה בדיוק לשורה הראשונה של כל מייל, ורק אם תואם ממשיך לטפל בתוכן. **אם צריך את הקוד — לבקש מהמשתמש, לא לשחזר/לנחש.**
   - **קופסת ההערה באתר יודעת רק לפתוח מייל — היא לא כותבת יותר ישירות ל-Firestore** (הוסר `saveNoteToFirebase`). זה שינוי מהותי מהתכנון המקורי: מקור האמת היחיד ל"הערה מאומתת" הוא המייל שעבר את בדיקת הקוד, לא כתיבה חופשית מהדפדפן הציבורי. `#noteLog` עדיין קורא מ-Firestore `notes`, אבל עכשיו רק הבוט (אחרי אימות) כותב לשם.
   - הבוט כותב תשובות ל-Firestore דרך **REST API רגיל** (`POST .../documents/notes`), בלי שום credential — כללי האבטחה כבר פתוחים ל-collection הזה.
   - **מלכודת שנתקלתי בה:** ה-skill "schedule" יוצר **routines בענן** (`RemoteTrigger`) — סשן מבודד לגמרי בלי גישה לקבצים מקומיים, בלי ה-git credentials המקומיים, ומינימום שעה בין הרצות. זה **לא מתאים** למשימות שצריכות קובץ סוד מקומי/push עם GCM כמו כאן. המשימה המתוזמנת הנכונה היא `mcp__scheduled-tasks__create_scheduled_task` — אותה מערכת בדיוק שמריצה את `japan-daily-flight-check`. אם בעתיד תתבקש עוד משימה מתוזמנת דומה — להשתמש ישר בכלי הזה, לא בסקיל "schedule".
   - **הרצה ידנית "עכשיו":** אפשר מתוך אזור "Scheduled" בסיידבר של Claude Code — בלי לבנות מנגנון נוסף.
5. **נשקל ונדחה בשלב זה:** צ'אט AI חי בתוך האתר (Cloudflare Worker + מפתח Anthropic API) — עלות אמיתית (אם כי קטנה) ומאמץ הקמה גדול יותר. אפשרי כשדרוג עתידי מעל אותו Firebase.

### ג. אירוח — GitHub Pages ✅ חי מ-09/08/2026
**כתובת האתר החי: https://ozs1976.github.io/japan-trip-2027/**

מצב: git מקומי ✅ · remote `ozs1976/japan-trip-2027` (ציבורי) ✅ · Pages מופעל ✅ · `index.html` מפנה ל-`japan_trip_planner.html` ✅.

- **חשוב לזכור ל-push עתידי:** `git push origin main` רגיל מספיק — Git Credential Manager (מותקן במחשב) פותח אימות בדפדפן בפעם הראשונה בכל session, המשתמש מאשר בעצמו. **לעולם לא לבקש טוקן/סיסמה מהמשתמש**, ולא לנסות לעקוף את זה.
- אחרי כל push, GitHub Pages בונה מחדש אוטומטית (כדקה). כדי לוודא שעדכון הגיע בפועל — להשתמש ב-`fetch(url, {cache:'no-store'})`, לא בניווט רגיל בדפדפן (שיכול להראות גרסה ישנה מה-cache המקומי של הדפדפן עצמו, גם כשהפריסה בפועל תקינה).
- **קובץ המקור האמיתי נשאר `japan_trip_planner.html`** (לא `index.html`) — כל שאר התיעוד/runbooks (FLIGHTS_TASK.md, המשימה המתוזמנת) ממשיכים להתייחס לשם הזה.

## 9. הוראות עבודה קבועות

- עברית תקנית + RTL מלא בכל טקסט שמופיע לעין. `ltr-num` על כל מספר מעורב.
- לעולם לא לסמוך על זיכרון בנוגע לוויזה / אזהרות מסע / מחירים — לאמת מול מקור עדכני.
- אחרי כל החלטה או שינוי מהותי: לעדכן `CLAUDE.md` + `DECISIONS.md` ולעשות commit.
- לא לייצר PDF.
- בנושאים הפתוחים בסעיף 7 — לשאול את המשתמש, לא להניח.
- **המשתמש רוצה שהתוצרים ייכנסו לקובץ ה-HTML, לא שיוצגו כטקסט בצ'אט.** כל ניתוח, השוואה או המלצה — לכתוב אותם לתוך הטאב הרלוונטי באתר. בצ'אט להסתפק בסיכום קצר של מה נעשה ואיפה זה נמצא.
